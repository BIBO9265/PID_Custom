# PID Custom

Arduino header-only PID library packaged from the supplied `PID.h`.

## Installation

In Arduino IDE, select **Sketch > Include Library > Add .ZIP Library...** and choose `PID_Custom.zip`.

## Include

```cpp
#include <PID.h>
```

See `examples/BasicUsage/BasicUsage.ino` for a minimal example.
